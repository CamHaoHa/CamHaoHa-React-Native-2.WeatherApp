import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
  textTheme: {
    fontWeight: 'bold',
  },
  container: {
    alignItems: 'center',
  },
});
export default styles;
